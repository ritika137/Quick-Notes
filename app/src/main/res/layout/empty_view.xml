<?xml version="1.0" encoding="utf-8"?>
<!-- Empty view for the list -->
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="90dp"
    tools:context="com.example.ritika.quicknotes.MainActivity">

        <ImageView
            android:id="@+id/empty_shelter_image"
            android:layout_width = "wrap_content"
            android:layout_height = "150dp"
            app:srcCompat="@drawable/notes"
            android:layout_marginStart="16dp"
            android:layout_alignStart="@+id/empty_title_text"
            android:layout_alignEnd="@+id/empty_title_text"
            />

        <TextView
            android:id="@+id/empty_title_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/empty_shelter_image"
            android:fontFamily="sans-serif-medium"
            android:paddingTop="16dp"
            android:text="It's a bit lonely here..."
            android:textAppearance="?android:textAppearanceMedium"
            android:layout_centerInParent="true"/>

        <TextView
            android:id="@+id/empty_subtitle_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/empty_title_text"
            android:layout_centerHorizontal="true"
            android:fontFamily="sans-serif"
            android:paddingTop="8dp"
            android:text="Get started by adding a new Note!"
            android:textAppearance="?android:textAppearanceSmall"
            android:textColor="#A2AAB0"
            android:layout_centerInParent="true"/>
    </RelativeLayout>
